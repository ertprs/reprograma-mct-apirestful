/*const mongoose = require('mongoose');
const Schema = mongoose.Schema;

const schema = new Schema({

    ******colocar aqui o qu irá compor a empresa*****
});

module.exports = mongoose.model('empresa', schema);*/
